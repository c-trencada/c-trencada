#inclou "test.cç"

ent principal() {
  AFIRMA(1, ({ car x; mida_de(x); }));
  AFIRMA(2, ({ curt ent x; mida_de(x); }));
  AFIRMA(2, ({ ent curt x; mida_de(x); }));
  AFIRMA(4, ({ ent x; mida_de(x); }));
  AFIRMA(8, ({ llarg ent x; mida_de(x); }));
  AFIRMA(8, ({ ent llarg x; mida_de(x); }));

  AFIRMA(8, ({ llarg llarg x; mida_de(x); }));

  AFIRMA(0, ({ _Bool x=0; x; }));
  AFIRMA(1, ({ _Bool x=1; x; }));
  AFIRMA(1, ({ _Bool x=2; x; }));
  AFIRMA(1, (_Bool)1);
  AFIRMA(1, (_Bool)2);
  AFIRMA(0, (_Bool)(car)256);

  imprimeix("OK\n");
  retorna 0;
}
