#inclou "test.cç"

enter x;
enter x = 5;
enter y = 7;
enter y;
enter common_ext1;
enter common_ext2;
estàtic enter common_local;

enter principal() {
  AFIRMA(5, x);
  AFIRMA(7, y);
  AFIRMA(0, common_ext1);
  AFIRMA(3, common_ext2);

  imprimeix("OK\n");
  retorna 0;
}
