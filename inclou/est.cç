#pragma una_vegada

#inclou <est_arg.cç>

enter imprimeix(const curt* restringeix fmt, ...);
enter imprimeix_va(const curt* restringeix fmt, llista_arguments_vars ap);
enter imprimeix_cadena(const curt* restringeix cadena, const curt* restringeix fmt, ...);
enter imprimeix_cadena_va(const curt* restringeix cadena, const curt* restringeix fmt, llista_arguments_vars ap);

buit* assigna_mem(sense_signe llarg size);
buit* reassigna_mem(buit *ptr, sense_signe llarg size);
buit allibera(buit *ptr);

buit* assigna_mem_auto(sense_signe llarg size);

buit* copia_mem(buit *dest, buit *origen, sense_signe llarg size);
enter compara_mem(buit *a, buit *b, sense_signe llarg size);

enter compara_cadenes(const curt *a, const curt *b);
enter compara_cadenes_fins(const curt *a, const curt *b, sense_signe llarg n);

enter longitud_cadena(const curt *cadena);

buit sortir(enter codi);

